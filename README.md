# Code-alpha-Task-1-Basic-Network-sniffer-
A network sniffer (or packet sniffer) is a software or hardware tool that intercepts and captures data packets traveling across a network. It works by placing a network interface into promiscuous mode (wired) or monitor mode (Wi-Fi), allowing it to capture all traffic on a segment rather than just packets addressed to it.
